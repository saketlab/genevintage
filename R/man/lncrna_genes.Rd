% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{lncrna_genes}
\alias{lncrna_genes}
\title{Long non-coding RNA genes}
\usage{
lncrna_genes(
  ids = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{id}, \code{name}, \code{chr} and \code{biotype}, shaped as
\code{\link[=mito_genes]{mito_genes()}} returns.
}
\description{
Collects every spelling the class has gone by, because the annotations do not
agree even with each other: at release 116 most species say \code{lncRNA}, but
zebrafish says \code{antisense} and \code{lincRNA} and chimpanzee says \code{lincRNA}, while
release 75 split it across \code{lincRNA}, \code{antisense}, \code{processed_transcript} and
\verb{3prime_overlapping_ncrna}.
}
\details{
Some annotations do not draw the distinction at all: Drosophila and yeast
file everything non-coding under a generic \code{ncRNA}, which is not the same
claim and is therefore not counted here. When that is the case this says so,
rather than returning an empty answer without explanation.
}
\examples{
\dontrun{
nrow(lncrna_genes(species = "human"))
table(lncrna_genes(species = "zebrafish")$biotype) # antisense and lincRNA
}
}
\seealso{
\code{\link[=noncoding_genes]{noncoding_genes()}} for every non-coding class, \code{\link[=gene_biotypes]{gene_biotypes()}}.
}
