% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{correct_rows}
\alias{correct_rows}
\title{Repair Excel-damaged row identifiers in place}
\usage{
correct_rows(
  x,
  ids = rownames(x),
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  unique = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A matrix or data frame with (possibly Excel-damaged) gene symbols
as row names.}

\item{ids}{Row identifiers. Defaults to \code{rownames(x)}.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{unique}{Disambiguate names that would otherwise collide, by appending
the damaged token. On by default: duplicate row names are legal in a
matrix but break almost everything downstream.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
\code{x} with new row names. Nothing else about the object changes: no
extra attributes, so a sparse \code{Matrix} stays a valid S4 object through
subsetting, binding and arithmetic. Keep \code{rownames(x)} yourself if you
need to see which tokens were ambiguous or unresolved; call
\code{\link[=correct_names]{correct_names()}} directly for that.
}
\description{
\code{\link[=correct_names]{correct_names()}} applied to a matrix's row names, and put back on the same
object. Dimensions never change and rows never move: a token with no
surviving candidate, or with more than one, keeps its original value.
}
\details{
Works on anything with \code{rownames()}: a base matrix, a sparse \code{Matrix} or a
data frame, because only the dimnames are touched.
}
\examples{
\dontrun{
m <- matrix(1:4, nrow = 2, dimnames = list(c("9-Sep", "TP53"), c("s1", "s2")))
correct_rows(m, species = "human", release = 116)
}
}
\seealso{
\code{\link[=correct_names]{correct_names()}} for the vector form and its attributes,
\code{\link[=rename_rows]{rename_rows()}} for the ID-to-name case.
}
