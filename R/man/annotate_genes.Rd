% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{annotate_genes}
\alias{annotate_genes}
\title{Everything the annotation knows about a set of genes}
\usage{
annotate_genes(
  ids,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{input}, \code{id}, \code{name}, \code{chr}, \code{biotype} and, where
the annotation carries them, \code{start}, \code{end}, \code{strand} and \code{span}. \code{span} is
the genomic extent, not the exonic length -- see \code{\link[=stream_gtf]{stream_gtf()}}.
}
\description{
Symbol, biotype and coordinates in one table, for genes given as identifiers
or as names. The caller's own token comes back in \code{input}, so the result
joins straight onto a results table or a feature frame.
}
\details{
An input matching nothing is kept with \code{NA} columns rather than dropped, and
a name matching several genes yields a row for each.
}
\examples{
\dontrun{
ann <- annotate_genes(rownames(counts))
res$biotype <- ann$biotype[match(rownames(res), ann$input)]
}
}
\seealso{
\code{\link[=gene_names]{gene_names()}} when one value per input is what you want.
}
