% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R, R/zzz-aliases.R
\name{sex_genes}
\alias{sex_genes}
\alias{SexGenes}
\title{Sex-chromosome genes}
\usage{
sex_genes(
  ids = NULL,
  which = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)

SexGenes(
  ids = NULL,
  which = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{which}{Restrict to one chromosome: \code{"X"}, \code{"Y"}, \code{"Z"} or \code{"W"}.
Defaults to every sex chromosome the annotation has.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Supply an annotation directly to bypass detection and
downloading. A data frame with at least \code{id}, \code{name}, \code{chr} and \code{biotype},
as \code{\link[=fetch_mapping]{fetch_mapping()}} returns; supports local GTFs and custom builds.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{id}, \code{name}, \code{chr} and \code{biotype}, one row per gene,
shaped as \code{\link[=mito_genes]{mito_genes()}} returns.
}
\description{
Sex chromosome names vary by clade: mammals use X and Y, birds Z and W,
and C. elegans X. Annotations without recognised sex chromosomes return
no rows.
}
\examples{
\dontrun{
sex_genes(species = "human", which = "Y") # Y-linked genes
table(sex_genes(rownames(counts))$chr) # X vs Y in your matrix
}
}
\seealso{
\code{\link[=mito_genes]{mito_genes()}}.
}
