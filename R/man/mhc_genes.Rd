% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R, R/zzz-aliases.R
\name{mhc_genes}
\alias{mhc_genes}
\alias{MHCGenes}
\title{Major histocompatibility complex genes}
\usage{
mhc_genes(
  ids = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)

MHCGenes(
  ids = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Supply an annotation directly to bypass detection and
downloading. A data frame with at least \code{id}, \code{name}, \code{chr} and \code{biotype},
as \code{\link[=fetch_mapping]{fetch_mapping()}} returns; supports local GTFs and custom builds.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{id}, \code{name}, \code{chr} and \code{biotype}, shaped as
\code{\link[=mito_genes]{mito_genes()}} returns.
}
\description{
The MHC, which human calls HLA. Every clade names it differently: \code{HLA-A} in
human, \code{H2-K1} in mouse, \code{mhc1uba} in zebrafish, so matching \code{"^HLA-"} finds
nothing outside primates.
}
\details{
Requires \code{protein_coding} and an MHC family name pattern. The patterns
exclude regional genes such as \code{B2M}, \code{TAP1}, \code{TAP2} and \code{MICA}.
Annotations without matching genes return an empty result.
}
\examples{
\dontrun{
mhc_genes(species = "human") # coding HLA genes
mhc_genes(species = "mouse") # H2-K1, H2-D1, H2-Aa, ...
}
}
\seealso{
\code{\link[=genes_by_biotype]{genes_by_biotype()}} for the immunoglobulin and T-cell receptor
segments, which are a different question.
}
