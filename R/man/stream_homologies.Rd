% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ortholog.R
\name{stream_homologies}
\alias{stream_homologies}
\title{Stream one species' orthologs against one other species}
\usage{
stream_homologies(url, to, swap = FALSE, chunk = 2e+05)
}
\arguments{
\item{url}{A Compara \code{homologies.tsv.gz} URL.}

\item{to}{Ensembl species name to keep, e.g. \code{"mus_musculus"}.}

\item{swap}{Return the pair the other way round. Compara's per-species files
are not symmetric: human's omits mouse while mouse's carries human, so
the pair sometimes has to be read from the other species' file and turned
around. Orthology is symmetric, so this loses nothing.}

\item{chunk}{Lines to decompress per iteration.}
}
\value{
A data frame of \code{id}, \code{ortholog_id}, \code{homology_type}, \code{identity},
\code{ortholog_identity}, \code{high_confidence}.
}
\description{
Compara publishes one homology file per species covering every other species
and every paralog, so almost all of it is other
species' business, so the target species is filtered out of the stream in
chunks and only that pair is kept, the way \code{\link[=stream_gtf]{stream_gtf()}} keeps only gene
lines. Peak memory stays small and nothing reaches disk but the result.
}
\examples{
\dontrun{
url <- paste0(
  "https://ftp.ensembl.org/pub/release-116/tsv/ensembl-compara/",
  "homologies/mus_musculus/",
  "Compara.116.protein_default.homologies.tsv.gz"
)
head(stream_homologies(url, to = "homo_sapiens"))
}
}
