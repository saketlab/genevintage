% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.R
\name{correct_names}
\alias{correct_names}
\title{Repair gene symbols a spreadsheet has converted to dates or numbers}
\usage{
correct_names(
  x,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  unique = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{Gene names, some of which a spreadsheet may have converted.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{unique}{Make the result unique by appending the damaged token to a
repaired name that collides with another (as \code{\link[=gene_names]{gene_names()}} does), then
falling back to \code{\link[=make.unique]{make.unique()}} for any collision that remains. Off by
default, since it changes names.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A character vector the same length and order as \code{x}, carrying a
\code{corrected} attribute (logical), an \code{ambiguous} attribute naming tokens
that matched more than one symbol, and an \code{unresolved} attribute naming
damaged tokens no symbol in this annotation produces.
}
\description{
Excel silently reinterprets some gene symbols: \code{SEPT9} becomes \code{9-Sep},
\code{MARCH1} becomes \code{1-Mar}, \code{2310009E13} becomes \code{2.31E+13}. The damage is
widespread in published supplementary files (Ziemann et al. 2016;
Abeysooriya et al. 2021) and is not recoverable from the file alone, because
several symbols can produce the same date.
}
\details{
The annotation resolves it. Every symbol that could have produced the token
is generated, and only those the caller's own species and release actually
contain are offered, so the answer is the symbol their matrix should hold.

That matters more than it sounds: HGNC renamed the worst offenders in 2020,
so \code{9-Sep} is \code{SEPT9} against Ensembl release 95 and \code{SEPTIN9} against
release 116. A fixed lookup table cannot be right for both.

Tokens that are not damaged are returned unchanged, so the result drops
straight back into \code{rownames()}. A token with no surviving candidate, or with
more than one, is also returned unchanged and reported.

A damaged token carries no species of its own, so pass \code{mapping}, or both
\code{species} and \code{release}, whenever \code{x} contains one. Choosing the release
explicitly also decides historical names such as \code{SEPT9} versus \code{SEPTIN9};
left to detect, a symbol-only vector would silently default to the newest
release instead of refusing to guess.
}
\examples{
\dontrun{
# against a 2019 vintage these come back as SEPT9 and MARCH1
correct_names(c("9-Sep", "1-Mar", "TP53"), species = "human", release = 95)

# against a current one, as SEPTIN9 and MARCHF1
correct_names(c("9-Sep", "1-Mar", "TP53"), species = "human", release = 116)
}
}
\references{
Ziemann M, Eren Y, El-Osta A (2016). Gene name errors are widespread in the
scientific literature. \emph{Genome Biology} 17:177.

Abeysooriya M, Soria M, Kasu MS, Ziemann M (2021). Gene name errors: Lessons
not learned. \emph{PLOS Computational Biology} 17(7):e1008984.
}
\seealso{
\code{\link[=gene_names]{gene_names()}}, \code{\link[=gene_ids]{gene_ids()}}.
}
