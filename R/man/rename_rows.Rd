% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename.R
\name{rename_rows}
\alias{rename_rows}
\title{Replace a matrix's row identifiers with gene names}
\usage{
rename_rows(
  x,
  ids = rownames(x),
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  unique = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A matrix or data frame with gene identifiers as row names.}

\item{ids}{Row identifiers. Defaults to \code{rownames(x)}.}

\item{species}{Skip species detection by naming it. Any form
\code{\link[=resolve_species]{resolve_species()}} accepts: \code{"homo_sapiens"}, \code{"Homo sapiens"},
\code{"Human"} or \code{"human"}.}

\item{release}{Skip release detection by naming it.}

\item{assembly}{Assembly suffix, e.g. \code{"37"} for human GRCh37. Detected
alongside the release when both are left \code{NULL}.}

\item{source}{Annotation family: \code{"ensembl"}, or \code{"gencode"} /
\code{"gencode_all"} for GENCODE's primary and scaffold-inclusive gene sets.
Detected alongside the release. Human and mouse counts are usually produced
against GENCODE, whose gene set is the smaller of the two.}

\item{mapping}{Skip detection entirely and use this \code{id}/\code{name} table, as
accepted by \code{\link[=gene_names]{gene_names()}}. Useful when the mapping is already in hand.}

\item{unique}{Disambiguate names that would otherwise collide, by appending
the identifier. On by default: duplicate row names are legal in a matrix
but break almost everything downstream.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
\code{x} with new row names. Nothing else about the object changes: no
extra attributes, so a sparse \code{Matrix} stays a valid S4 object through
subsetting, binding and arithmetic. Keep \code{rownames(x)} yourself if you
need to undo the rename.
}
\description{
Detects and converts the row identifiers, then puts the names back on the
same object. Dimensions never change and rows never move: a row whose
identifier has no gene name keeps the identifier.
}
\details{
Works on anything with \code{rownames()}: a base matrix, a sparse \code{Matrix} or a
data frame, because only the dimnames are touched.
}
\examples{
\dontrun{
m <- matrix(1:6,
  nrow = 3,
  dimnames = list(c(
    "ENSG00000141510", "ENSG00000284733",
    "ENSG00000012048"
  ), c("s1", "s2"))
)
rename_rows(m)
#>                 s1 s2
#> TP53             1  4
#> ENSG00000284733  2  5
#> BRCA1            3  6
}
}
\seealso{
\code{\link[=geneid2name]{geneid2name()}} for the vector form.
}
