% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{ribosomal_genes}
\alias{ribosomal_genes}
\title{Ribosomal genes}
\usage{
ribosomal_genes(
  ids = NULL,
  which = c("protein", "rrna", "mito", "all"),
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{which}{Which ribosomal set to return. See details.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{id}, \code{name}, \code{chr} and \code{biotype}, shaped as
\code{\link[=mito_genes]{mito_genes()}} returns.
}
\description{
Two different sets go by this name, so \code{which} says which one:
}
\details{
\itemize{
\item \code{"protein"} (the default): the genes encoding the ribosome's proteins,
the set behind a single-cell ribosomal-content metric.
\item \code{"rrna"}: the RNA components, by biotype (\code{rRNA} and \code{Mt_rRNA}).
\item \code{"mito"}: the mitoribosome's proteins, named with an \code{M} prefix. These are
nuclear genes; \code{\link[=mito_genes]{mito_genes()}} does not return them.
\item \code{"all"}: every one of the above.
}

Two filters beyond the \code{"^RP[SL]"} symbol pattern. Most genes carrying the
name are ribosomal protein pseudogenes and lncRNAs, which the \code{protein_coding}
biotype removes. The ten S6 kinases (\code{RPS6KA1} to \code{RPS6KL1}) and \code{RPS19BP1}
survive that, and a name rule removes them.
}
\examples{
\dontrun{
rb <- ribosomal_genes(rownames(counts))
colSums(counts[rb$input, ]) / colSums(counts) # ribosomal content
ribosomal_genes(species = "human", which = "rrna")
}
}
\seealso{
\code{\link[=mito_genes]{mito_genes()}}, \code{\link[=genes_by_biotype]{genes_by_biotype()}}.
}
