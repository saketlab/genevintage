% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{gene_ids}
\alias{gene_ids}
\title{Gene identifiers for gene names or Entrez identifiers}
\usage{
gene_ids(
  x,
  from = c("auto", "name", "entrez"),
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{Gene names, or Entrez identifiers.}

\item{from}{What \code{x} holds: \code{"name"}, \code{"entrez"}, or \code{"auto"} to decide from
the input. All-digit input is Entrez; anything else is a name.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{input}, \code{id} and \code{name}, one row per match.
}
\description{
The reverse of \code{\link[=gene_names]{gene_names()}}. Published marker lists and enrichment results
carry symbols or Entrez identifiers; count matrices carry Ensembl ones.
}
\details{
Neither input is a key. A symbol can name several genes, and an Entrez
identifier can cross-reference several, so every match is returned and the
result is a table rather than a vector. An input matching nothing is kept
with \code{id = NA}, so nothing disappears silently.
}
\examples{
\dontrun{
gene_ids(c("TP53", "BRCA1"), species = "human")
gene_ids(c("7157", "672"), from = "entrez", species = "human")
}
}
\seealso{
\code{\link[=gene_names]{gene_names()}} for the forward direction, \code{\link[=entrez_ids]{entrez_ids()}}.
}
