% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{mito_genes}
\alias{mito_genes}
\title{Mitochondrial genes}
\usage{
mito_genes(
  ids = NULL,
  species = NULL,
  release = NULL,
  assembly = NULL,
  source = NULL,
  mapping = NULL,
  quiet = FALSE
)
}
\arguments{
\item{ids}{Optional character vector of gene identifiers or gene names. When
given, the species and release are detected from them and only these genes
are considered. When omitted, \code{species} must be named and the whole
mitochondrial gene set for its newest indexed release is returned.}

\item{species, release, assembly, source}{Skip detection by naming them.
\code{species} accepts anything \code{\link[=resolve_species]{resolve_species()}} does.}

\item{mapping}{Use this annotation instead of fetching one: a local GTF, a
custom build, or a species the index does not cover. A data frame with at
least \code{id}, \code{name}, \code{chr} and \code{biotype}, as \code{\link[=fetch_mapping]{fetch_mapping()}} returns.
Nothing is detected or downloaded when it is supplied.}

\item{quiet}{Suppress the message reporting what was detected.}
}
\value{
A data frame of \code{id}, \code{name}, \code{chr} and \code{biotype}, one row per
mitochondrial gene. When \code{ids} were given, an \code{input} column comes first
holding the caller's own token, identifier or gene name as passed, so
\code{rownames(m) \%in\% mito_genes(rownames(m))$input} subsets a
matrix directly whichever kind its row names are.
}
\description{
The mitochondrial fraction of a cell's counts is the standard single-cell
quality signal, and finding those genes means knowing which sequence the
mitochondrion is called in this species' annotation, which is not the same
string twice. Matching on the sequence name is exact; matching gene symbols
against \code{"^MT-"} misses every species that does not use that convention and
catches nuclear genes such as \code{MTOR} that merely start with the letters.
}
\examples{
\dontrun{
mito_genes(species = "human") # the mitochondrial genes
mt <- mito_genes(rownames(counts)) # just those in your matrix
colSums(counts[mt$input, ]) / colSums(counts) # the mitochondrial fraction
}
}
\seealso{
\code{\link[=sex_genes]{sex_genes()}} for the sex chromosomes.
}
